name: Post to OpenAI Thread

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Don't call the API, just print the payload"
        required: false
        default: "false"
  schedule:
    - cron: "0 * * * *"  # hourly run

permissions:
  contents: write

jobs:
  post:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      OPENAI_THREAD_ID: ${{ secrets.OPENAI_THREAD_ID }}
      OPENAI_ASSISTANT_ID: ${{ secrets.OPENAI_ASSISTANT_ID }}
      SBX_FEED_URL: ${{ secrets.SBX_FEED_URL }}

    steps:
      - name: Sanity checks
        run: |
          set -euo pipefail
          test -n "$OPENAI_API_KEY" || { echo "❌ Missing OPENAI_API_KEY"; exit 1; }
          test -n "$OPENAI_THREAD_ID" || { echo "❌ Missing OPENAI_THREAD_ID"; exit 1; }
          test -n "$OPENAI_ASSISTANT_ID" || { echo "❌ Missing OPENAI_ASSISTANT_ID"; exit 1; }
          echo "✅ Core secrets OK."
          [ -n "${SBX_FEED_URL:-}" ] && echo "✅ SBX_FEED_URL OK." || echo "⚠️ No SBX_FEED_URL (feed step will skip)."

      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Compose base message
        id: compose
        shell: bash
        run: |
          set -euo pipefail
          TS="$(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          LAST="$(tail -n 1 handover/SBX_HandoVER.md 2>/dev/null || true)"
          [ -z "$LAST" ] && LAST="(no last line found in handover/SBX_HandoVER.md)"
          printf 'SBX handover sync — %s\n%s\n' "$TS" "$LAST" > /tmp/body.txt
          echo "✅ Base message ready"

      - name: Fetch SBX live feed
        if: env.SBX_FEED_URL != ''
        shell: bash
        run: |
          set -euo pipefail
          echo "📡 Fetching SBX feed from $SBX_FEED_URL"
          HTTP=$(curl -sS -w '%{http_code}' -o /tmp/feed.json "$SBX_FEED_URL" || true)
          echo "HTTP $HTTP"
          if [ "$HTTP" != "200" ]; then
            echo "⚠️ Feed fetch failed ($HTTP)" >> /tmp/body.txt
            exit 0
          fi

          {
            echo ""
            echo "---"
            echo "SBX Feed Snapshot (first 5 rows):"
            jq -r '
              if has("data") then .data else . end
              | .[0:5]
              | map(
                  (.symbol // .ticker // "-")
                  + "\t" + (.grade // "-")
                  + "\t" + (.bias // "-")
                  + "\t" + (.setup // "-")
                  + "\t" + (.session // "-")
                )
              | join("\n")
            ' /tmp/feed.json 2>/dev/null || head -n 40 /tmp/feed.json
          } >> /tmp/body.txt

          mkdir -p handover
          cp /tmp/feed.json handover/SBX_Feed_Snapshot.json
          git add handover/SBX_Feed_Snapshot.json || true
          git commit -m "ci: SBX feed snapshot" || true
          echo "✅ SBX feed summary appended."

      - name: Verify thread exists
        run: |
          set -euo pipefail
          curl -sS -w '\nHTTP:%{http_code}\n' \
            -H "Authorization: Bearer $OPENAI_API_KEY" \
            -H "OpenAI-Beta: assistants=v2" \
            "https://api.openai.com/v1/threads/${OPENAI_THREAD_ID}" \
            | tee /tmp/thread_check.txt
          grep -q 'HTTP:200' /tmp/thread_check.txt || { echo "❌ Thread not found"; exit 1; }

      - name: Dry run check
        if: ${{ github.event.inputs.dry_run == 'true' }}
        run: |
          echo "Dry run enabled:"
          jq -nc --rawfile t /tmp/body.txt '{role:"user", content:[{type:"text", text:$t}]}' | tee payload.json
          exit 0

      - name: Post message to OpenAI
        id: post
        run: |
          set -euo pipefail
          jq -nc --rawfile t /tmp/body.txt '{role:"user", content:[{type:"text", text:$t}]}' > /tmp/payload.json
          HTTP=$(curl -sS -o /tmp/resp.json -w '%{http_code}' \
            -H "Authorization: Bearer $OPENAI_API_KEY" \
            -H "Content-Type: application/json" \
            -H "OpenAI-Beta: assistants=v2" \
            "https://api.openai.com/v1/threads/${OPENAI_THREAD_ID}/messages" \
            --data @/tmp/payload.json)
          echo "HTTP: $HTTP"
          [ "$HTTP" = "200" ] || { cat /tmp/resp.json; exit 1; }
          echo "✅ Message posted."

      - name: Create run
        id: run
        run: |
          set -euo pipefail
          echo "Creating run..."
          HTTP=$(curl -sS -o /tmp/run.json -w '%{http_code}' \
            -H "Authorization: Bearer $OPENAI_API_KEY" \
            -H "Content-Type: application/json" \
            -H "OpenAI-Beta: assistants=v2" \
            -d "{\"assistant_id\":\"$OPENAI_ASSISTANT_ID\"}" \
            "https://api.openai.com/v1/threads/${OPENAI_THREAD_ID}/runs")
          echo "HTTP: $HTTP"
          [ "$HTTP" = "200" ] || { cat /tmp/run.json; exit 1; }
          RUN_ID=$(jq -r '.id' /tmp/run.json)
          echo "RUN_ID=$RUN_ID" >> "$GITHUB_OUTPUT"
          echo "✅ Run started."

      - name: Wait for completion
        run: |
          set -euo pipefail
          RUN_ID="${{ steps.run.outputs.RUN_ID }}"
          echo "Waiting for run $RUN_ID..."
          for i in $(seq 1 100); do
            STATUS=$(curl -sS \
              -H "Authorization: Bearer $OPENAI_API_KEY" \
              -H "OpenAI-Beta: assistants=v2" \
              "https://api.openai.com/v1/threads/${OPENAI_THREAD_ID}/runs/${RUN_ID}" \
              | jq -r '.status')
            echo "Status: $STATUS"
            [ "$STATUS" = "completed" ] && break
            [ "$STATUS" = "failed" -o "$STATUS" = "cancelled" ] && exit 1
            sleep 3
          done

      - name: Get assistant response
        id: response
        run: |
          set -euo pipefail
          curl -sS \
            -H "Authorization: Bearer $OPENAI_API_KEY" \
            -H "OpenAI-Beta: assistants=v2" \
            "https://api.openai.com/v1/threads/${OPENAI_THREAD_ID}/messages?order=desc&limit=5" > /tmp/messages.json
          jq '.' /tmp/messages.json | head -120
          REPLY=$(jq -r '.data[] | select(.role=="assistant") | .content[0].text.value' /tmp/messages.json | head -n 1)
          echo "Assistant reply:"
          echo "$REPLY"

      - name: Summary
        run: |
          echo "✅ Complete"
